# Orthogonal Module Feature Encoding

Trait ASR should operate on orthogonal features rather than the legacy `C_tail` residual bin.

- Total sequences recoded: 9393
- Structure panel entries for calibration: 35
- `c_residual` is explicitly marked secondary/conditional.
